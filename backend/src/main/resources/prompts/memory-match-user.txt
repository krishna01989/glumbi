<task>Create 6 emoji+label pairs for a <theme>"%s"</theme> themed memory match game for <child_name>%s</child_name> (age <age>%d</age>).</task>

<rules>
  <rule>Make sure all 6 pairs are distinct</rule>
  <rule>Use simple, recognizable emojis</rule>
</rules>

<output>Return ONLY this JSON:
[{"emoji":"🐶","label":"Dog"},...]</output>
